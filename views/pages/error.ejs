<!DOCTYPE html>
<html lang="it">

<%- include('../partials/header') %>

  <body>
    <%- include('../partials/navbar') %>

      <main>
        <section class="error-section d-flex align-items-center">
          <div class="container position-relative text-center">
            <div class="mb-4">
              <i class="fas fa-exclamation-circle fa-4x text-info"></i>
            </div>
            <h1 class="display-4 fw-bold">Errore</h1>
            <p class="lead mb-4"> <%= Array.isArray(error) ? error[0] : error %> </p>
            <a href="/" class="btn btn-primary btn-lg">Torna alla Home</a>
          </div>
        </section>
      </main>

      <%- include('../partials/footer') %>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>

</html>
